import React, { useState } from "react";

const LocationSelect = ({ setIsLocationSelected }) => {
  return (
    <div>
      <center>Pasirinkite vietove</center>

      {/* {booksdb.map((book) => (
        <Card book={book} key={book.id} />
      ))} */}
    </div>
  );
};

export default LocationSelect;
